#pragma once
#ifndef DRAKO_VULKAN_UNIFORM_BUFFER
#define DRAKO_VULKAN_UNIFORM_BUFFER

#include <vulkan/vulkan.hpp>

namespace drako::gpx
{
    class uniform_buffer
    {
    public:
        explicit uniform_buffer();

    private:
    };

    uniform_buffer::uniform_buffer()
    {
    }

} // namespace drako::gpx

#endif // !DRAKO_VULKAN_UNIFORM_BUFFER